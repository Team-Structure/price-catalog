image: node:latest

cache:
  paths:
    - node_modules/

services:
  - mongo:latest

variables:
  MONGO_URI: 'mongodb://mongo/seller-catalog'

stages:
  - build
  - test

build:
  stage: build
  script:
    - npm install
    - npm run seed

test:
  stage: test
  script:
    - npm run start
    - npx mocha specs/spec.js
  after_script:
    - pkill node
